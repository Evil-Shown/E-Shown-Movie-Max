OMDB_API_KEY=a655787b
TMDB_API_KEY=54fff1e340b9336a504dca9815a8883e

# Search API base URL (defaults to http://localhost:5000)
NEXT_PUBLIC_GODS_EYE_API_URL=http://localhost:5000
NEXT_PUBLIC_TBOOM_API_URL=http://localhost:5000

# Site branding (CHITHRA — CINEMA / චිත්‍ර — Cinema)
NEXT_PUBLIC_SITE_NAME=CHITHRA — CINEMA

# Legacy API base (used if TBOOM_API_URL unset)
NEXT_PUBLIC_API_BASE_URL=http://localhost:5000

# Route stream iframes through backend embed proxy (off by default — direct embeds are more reliable)
# NEXT_PUBLIC_USE_EMBED_PROXY=true
NEXT_PUBLIC_API_BASE_URL=http://localhost:5000

NEXT_PUBLIC_USE_EMBED_PROXY=false

# Desktop startup — optional override for the 3D Spline carousel (defaults to built-in my.spline.design URL)
# NEXT_PUBLIC_SPLINE_STARTUP_SCENE=https://my.spline.design/your-scene/
# Or Export → Code → React: https://prod.spline.design/xxxxxxxx/scene.splinecode